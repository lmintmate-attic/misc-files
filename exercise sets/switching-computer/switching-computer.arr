fun switching-computer(new-messages):
  doc: "Program that decides whether the computer should be turned on, depending on whether there are new messages in the inbox."
  if new-messages == true:
    "Must turn the computer on."
  else:
    "No need to turn the computer on."
  end
end